# WebComponents

The first component simple counter which have mode: open and closed.
The second component has two slots and controls the counter using a toggle

Deploy https://noisekov.github.io/webComponents/